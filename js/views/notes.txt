BUGS!
Removing a timezone removes the 1st one in the list - maybe index reading as string

when switching between mg/dL and mmol/L, need toggle to change eg the Reference food value in mmol/L also

for arrays there are 3 search functions. find() is for 1 element/value. filter() is to return multiple objects matching criteria. forEach for more custom logic
const myTrend = trends2.find(obj => obj.id === 'low')?.toTrack; // Using ? for safety - means won't crash if not found


//------------------------------------------------------
getElementById for insulin-name-1 = 
<input type="text" id="insulin-name-1">
you can directly access value in this
but with queryselector all cannot access straight away

queryselectorall - use .class (CSS) then can put in an element eg input
test out first by console-logging element with id
then try and get same console-log using querySelectorAll[0] - it can be done
but remember need to get to forEach or index[0] to change it

//------------------------------------------------------
listeners should only be added once - or they run exponentially!
